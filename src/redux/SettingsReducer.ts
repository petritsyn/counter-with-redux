type InitialStateType = {}

const initialState = {}

export const SettingsReducer = (state: InitialStateType = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}